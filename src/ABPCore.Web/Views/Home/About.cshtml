@using ABPCore.Web.Startup
@model List<ABPCore.BPEC_RetailItem>
@{
    ViewBag.ActiveMenu = PageNames.About;
}

<h2>@L("About")!</h2>

<div class="row">
    <div class="col-lg-12">
        @L("About_Description")
        
        @foreach (var item in Model)
        {
            @item.ItemNo 
            <br />
        }
    </div>
</div>